apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: prd
namePrefix: prd-

configMapGenerator:
  - name: manim-config
    literals:
      - ENVIRONMENT="Producción (PRD)"
      - MANIM_QUALITY="-qh"
